version: '3'
services:
  crawler:
    build: .
    image: mrgrodo/spider
    command: python main.py
    env_file:
      - .env
    volumes:
      - ".:/otodom-crawler"
    links:
      - redis
  redis:
    image: redis:latest
